defmodule RankingPage do

  # sends the request to the correct url
  def call do
    # result = url
    # |> build_url
    # |> fetch_data
    # |> parse_html
    # Then pass the result back below
  end

  # builds the correct URL to send requests to

  # Constant here of:
  # http://www.atpworldtour.com/en/rankings/singles?rankDate=2018-01-15&rankRange=1-5000&countryCode=all
  def build_url do
    # returns the correct url with the last monday adjusted correctly
  end


  # gets the last monday
  # returns it as text
  def last_monday(date) do

  end
end
